#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "Running frontend production build..."
npm run build:prod --workspace=frontend || { echo "Frontend build failed — commit aborted."; exit 1; }
